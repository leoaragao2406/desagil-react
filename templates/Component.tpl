import React from 'react';

import { View } from 'react-native';

import styles from './{name}.json';

export default function {name}(props) {
    return (
        <View style={styles.container}>
        </View>
    );
}
